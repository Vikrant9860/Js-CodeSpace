console.log(null ==0);   // false
console.log(null > 0 );   // false
console.log(null >= 0);   // true  
// comparisons >,<,>=,<=   convert the null to a number treating it as a 0
// thats why the answer is true 

console.log(undefined == 0);
console.log(undefined > 0);
console.log(undefined < 0);
console.log(undefined >= 0);
// In the case of undefined it will always give the false answer


// ===     triple equals to  // Strict Check
console.log('2' === 0);  // Here it not only checks the value
//  but the strictly datatype also 






